version: "3.9"
services:
  web:
    build: .
    ports:
      - "8000:8000"
      
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443" #Если используете HTTPS
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - web
